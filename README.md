A pomodoro timer built with [Create React App](https://github.com/facebook/create-react-app).

### CLI Commands

In the project directory, you can run all the standard Create React App scripts:
* `yarn start`
* `yarn test`
* `yarn build`
* `yarn eject`